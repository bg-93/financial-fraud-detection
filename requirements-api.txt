# Runtime-only dependencies for the FastAPI container.
fastapi>=0.115,<1
pydantic>=2.9,<3
uvicorn[standard]>=0.30,<1
numpy==2.5.2
pandas==2.3.3
scikit-learn==1.9.0
# The CPU distribution exposes the same `xgboost` module without bundling the
# hundreds-of-megabytes NVIDIA runtime that the general Linux wheel includes.
xgboost-cpu==3.4.1
joblib==1.5.3
